# 当前阶段收尾与改版基线

现有功能已完成本轮回归和剩余库存页面验收。按用户要求暂停新增管理业务，先保留当前版本，后续根据指定模块进行调整。本记录是阶段性交接，不代表整个课程项目已全部交付。

## 已实现范围

| 模块 | 当前能力 |
| --- | --- |
| 认证 | 注册、登录、身份恢复、权限守卫、退出和账号状态隔离 |
| 装备商城 | 搜索筛选、系列、排序分页、详情、图片及售罄状态 |
| 购物车 | 游客本地保存、用户服务器购物车、登录幂等合并、数量维护和删除 |
| 用户订单 | 确认、事务下单、库存占用、商品快照、模拟支付、取消返库、历史查询和未知结果恢复 |
| 管理基础 | 独立布局、管理员权限、受保护接口入口、未实现功能明确占位 |
| 管理装备 | 真实列表与详情、新增、资料编辑、上下架及旧表单冲突保护 |
| 管理库存 | 独立增减、库存边界保护、事务回执、跨刷新恢复和重试去重 |

软删除及未完成订单保护、管理用户查询/冻结、管理订单查询/取消/交付仍未实现，其预留业务接口保持 501。真实支付、退款、自动取消、找回密码、上传和库存历史页面也不属于本次已完成范围。

## 本轮验证与修整

- `npm test`：后端 44/44、前端 107/107；`npm run test:db`：75/75，合计 226 项全部通过。
- `npm run build` 通过，数据库完整结构检查为 ready。本轮未修改业务库数据或结构。
- 库存页面补验请求超时、刷新、原操作重试和完成后清理：27 + 3 仅执行一次，最终保持 30；截图检查通过。具体记录见 [Stage 6.5](15-admin-stock-adjustment.md)。
- 修正初始化成功提示：装备数量从实际写入结果读取，避免仍显示旧的 16 件；现有种子为 22 件。没有重新初始化或重灌业务库。
- 其余模块沿用阶段记录中的页面验收，本次全部重跑自动回归，没有宣称重做所有历史页面场景或完整设备兼容验收。

## 环境与工作树

- 功能基线是 `feat/admin` 上的 `56a11a9`，本轮收尾作为后续独立提交保存，不改写已有历史、不合并或推送远端。
- 当前只有主工作树，没有额外工作树需要移除；用户配置、依赖和构建目录保持原有忽略规则。
- 验收使用独立临时库 `game_store_stock_preview_e665bf183ca5`，现已删除，测试延迟触发器一并清理。
- 本轮 3101/5174 预览进程已停止，测试账号已退出，测试页面已关闭。其他既有服务未处理。

## 后续大改时保留的约束

先明确要修改的模块、目标体验及数据兼容要求，再从这一提交建立独立修改分支。数据库变更采用保留数据的迁移，不通过重灌演示数据代替升级。

无论页面如何调整，都需保留服务器权限和金额校验、订单快照、事务内库存一致性，以及合并/订单/库存操作的重试编号。未知结果未确认前不能清除原操作或更换编号；账号切换后旧请求不得回写新账号。已有回归是后续调整时的检查依据。

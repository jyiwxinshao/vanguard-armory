# Stage 6.3 新增装备

接通 `/admin/equipments/new` 与 `POST /api/admin/equipments`。列表提供新增入口，保存成功进入新装备详情；本轮不实现编辑、调库存或删除，无数据库结构变更。

## 字段与保存规则

- 必填 name（去空白后 1–50 字）、price（1–1000000 整数分）、rarity、category、image。未知字段拒绝，不能传 id、管理员身份或 deleted 状态。
- attack/defense/stock 为 0–4294967295 整数，省略默认 0；stock 仅是创建时的初始库存。status 省略默认 off_sale，允许明确选择 on_sale。
- description 最多 500 字，series_code 最多 64 字，省略/null/空白保存为空；new_until 可空，否则要求严格 UTC ISO 时间 `YYYY-MM-DDTHH:mm:ss.000Z`，拒绝无效日期。
- 图片限定 `/images/equipments/` 下的简单文件名（字母数字、横线和下划线）及 png/webp/jpg/jpeg/svg 后缀。前端列出已有资源并预览，尚不提供上传。API 校验路径格式，不检查文件存在性；图片失效由既有组件降级显示。
- 前端以元填写，使用字符串拆分换算为分，拒绝超过两位小数，避免浮点四舍五入。新品时间按浏览器本地时区填写并转为 UTC。
- 后端复用管理员认证、权限与 no-store；验证通过后，在同一连接的事务内 INSERT 并读取新记录，成功提交后返回 201 和完整详情对象。失败回滚，金额与库存均按服务端规则验证。

## 提交与失败处理

前端提交期间禁用表单并同步拦截重复点击，成功后记录新 ID，再跳转详情；导航异常仍保留查看已创建装备的入口。卸载取消客户端请求，旧账号响应不回写。

422 保留表单内容并展示字段错误；无响应或 5xx 属于结果不确定，当前表单停止再次提交，提示返回列表核对是否已创建。取消客户端请求不代表服务端回滚。当前 POST **没有跨刷新幂等回执**，相同请求再次发送会新建另一件装备；页面不自动重试，结果不确定时需先核对列表。未保存的表单不持久化，离开页面或刷新需重新填写。

## 文件入口与后续

- `server/src/modules/admin/equipments/equipment.validation.js`：新增字段白名单、数值/枚举/日期/路径校验。
- 同目录 `equipment.service.js`：事务保存与返回详情；Router 复用已有 POST 路由。
- `client/src/views/admin/EquipmentCreate.vue`：新增表单、图片预览和保存状态；编辑页继续占位。
- `client/src/utils/admin/equipment-form.js`：金额/输入转换与不确定结果判断。
- 后端 API/真实 MySQL 测试及 `client/tests/admin-equipment-form.test.js`：输入边界、权限、金额、可见性和事务回滚。

下一小步实现资料编辑与上下架；编辑不能接收 stock，库存变化另行通过独立接口实现。

## 验证（2026-09-16）

- 后端 42/42、前端 103/103、数据库 71/71 全部通过；构建与 diff 检查通过。
- 浏览器验证新增入口、空表单校验、图片预览、真实保存、详情跳转与刷新。0.29 元、7 件初始库存、系列、新品时间、多行介绍保存正确；默认下架。
- 真实保存仅在本轮独立临时库进行，完成后退出测试账号、关闭页面、停止 3101/5174 预览并删除该库；业务库、已有服务及 `.env` 未修改。
